"const add = require('../index'); test('adds 1 + 2 to equal 3', () => { expect(add(1, 2)).toBe(3); });" 
const { square } = require('../index'); test('square 3 to be 9', () => { expect(square(3)).toBe(9); });
